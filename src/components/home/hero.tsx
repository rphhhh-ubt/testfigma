export function Hero() {
  return (
    <section className="hero" aria-labelledby="agenda-title">
      <h1 id="agenda-title" className="hero-title">
        agenda
      </h1>
      <p className="hero-subtitle">
        Choose events and become part of a unique cultural experience
      </p>
    </section>
  );
}
